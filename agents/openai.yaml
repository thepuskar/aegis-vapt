interface:
  display_name: "Aegis VAPT"
  short_description: "Adaptive enterprise VAPT across software and platforms"
  default_prompt: "Use $aegis-vapt to build the appropriate enterprise assessment profile, validate material attack paths, and produce an evidence-led VAPT report."

policy:
  allow_implicit_invocation: true
